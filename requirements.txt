# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# HTTP 요청
httpx==0.27.2
requests==2.32.3

# 웹 크롤링
beautifulsoup4==4.12.3
lxml==5.3.0
selenium==4.26.1
webdriver-manager==4.0.2

# 데이터 처리
pydantic==2.10.3
python-dateutil==2.9.0

# 환경변수 관리
python-dotenv==1.0.1

# 로깅
loguru==0.7.2

# 메시지 브로커 (RabbitMQ)
pika==1.3.2

# ORM (PostgreSQL 직접 저장)
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# ── Phase 2: AI/NLP 분석 ──────────────────────────────────────────
# 한국어 형태소 분석 (Java 의존성 없는 순수 Python 구현)
# KoNLPy 대신 kiwipiepy를 선택한 이유:
#   Python 3.12 + M-chip Mac에서 Java/JVM 없이 동작, pip 한 줄로 설치 완료
kiwipiepy==0.22.2

# TF-IDF 키워드 추출 (KeywordExtractor에서 사용)
scikit-learn==1.5.2

# 수치 연산 (scikit-learn 의존성, TF-IDF 행렬 처리)
numpy==2.1.3